# ╔════════════╗
# ║ My aliases ║
# ╚════════════╝
#
# aliases working on most the shells
#
# in this file there are only the files strictly related 
# to the os operations

alias la='ls -a'
alias ll='ls -l'

#alias rm='safe-rm'
alias rm='rm -i'

alias dfree='df -h | grep -v tmpfs'
alias sudo='sudo '

# ┌──────────────┐
# │ more aliases │
# └──────────────┘

if [ -f ~/.sh/extra.aliases ]; then
    source ~/.sh/extra.aliases
fi



